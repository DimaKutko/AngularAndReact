export default class Car {
    photo: string;
    model: string;
    year: number;

    constructor( photo: string, model: string, year: number) {
        this.photo = photo;
        this.model = model;
        this.year = year;
      }
}

